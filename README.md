# 🖥️ Системный монитор Linux Mint

Мониторинг системы в реальном времени с веб-интерфейсом и поддержкой Intel/NVIDIA GPU.

## ✨ Возможности

- 📊 Мониторинг CPU (Intel) с отображением модели, загрузки и частоты
- 🧠 Мониторинг оперативной памяти (использование, общий объем, процент)
- 🎨 Мониторинг Intel GPU (загрузка графического процессора)
- 🚀 Мониторинг NVIDIA GPU (загрузка, память, температура)
- 🌐 Веб-интерфейс с автообновлением каждые 2 секунды
- 📱 Адаптивный дизайн с интуитивными прогресс-барами
- 🐳 Поддержка Docker-контейнеризации

## 📁 Структура проекта

```
system-monitor/
├── system_monitor.py      # Основной скрипт мониторинга
├── web_monitor.py         # Веб-сервер с Flask
├── requirements.txt       # Зависимости Python
├── Dockerfile            # Конфигурация Docker
├── README.md             # Документация
└── .dockerignore         # Исключения для Docker
```

## 🚀 Быстрый старт

### Установка с помощью Git

```bash
# Клонируйте репозиторий
git clone https://github.com/avkolchin1995/system-monitor.git
cd system-monitor

# Установите зависимости
pip install -r requirements.txt

# Для мониторинга NVIDIA GPU установите дополнительные пакеты:
sudo apt-get install nvidia-smi

# Для мониторинга Intel GPU:
sudo apt-get install intel-gpu-tools
```

### Запуск в терминале

```bash
# Терминальный режим (обновление каждые 0.5 секунд)
python system_monitor.py
```

### Запуск веб-интерфейса

```bash
# Веб-интерфейс на http://localhost:5000
python web_monitor.py
```

### Запуск в Docker

```bash
# Сборка образа
docker build -t system-monitor .

# Запуск контейнера
docker run -d \
  --name system-monitor \
  -p 5000:5000 \
  --privileged \
  -v /sys:/sys:ro \
  -v /proc:/proc:ro \
  system-monitor

# Откройте в браузере: http://localhost:5000
```

## 🔧 Требования к системе

- **ОС**: Linux Mint (также работает на других дистрибутивах Linux)
- **Python**: 3.8 или выше
- **Процессор**: Intel (с поддержкой чтения /proc/cpuinfo)
- **GPU**: Intel и/или NVIDIA (опционально)

## 📊 Отслеживаемые метрики

### CPU (Intel)
- Название и модель процессора
- Количество ядер и потоков
- Загрузка процессора (%)
- Текущая частота (MHz)

### Оперативная память
- Общий объем памяти (GB)
- Использованная память (GB)
- Процент использования (%)

### GPU Intel
- Название графического процессора
- Загрузка GPU (%)
- (При наличии) Использование видеопамяти

### GPU NVIDIA
- Название и модель GPU
- Загрузка GPU (%)
- Использование видеопамяти (GB)
- Температура GPU (°C)

## 🐳 Docker-контейнер

Контейнер основан на `python:3.12-slim` и включает:

- Все необходимые Python-зависимости
- Системные утилиты для мониторинга (`pciutils`, `lshw`)
- Веб-сервер на Flask с автообновлением
- Привилегированный режим для доступа к системным данным

### Переменные окружения

- `HOST` - Хост для Flask (по умолчанию: 0.0.0.0)
- `PORT` - Порт для Flask (по умолчанию: 5000)

### Монтирование томов

Для корректной работы необходимы:
- `/sys` - информация о системе (только чтение)
- `/proc` - информация о процессах (только чтение)

## 🔍 Устранение неполадок

### Нет данных о GPU
```bash
# Проверьте наличие GPU
sudo lshw -C display -short

# Для Intel GPU установите инструменты
sudo apt-get install intel-gpu-tools

# Для NVIDIA проверьте драйверы
nvidia-smi
```

### Ошибки доступа в Docker
Убедитесь, что контейнер запущен с правами `--privileged` и правильными монтированиями.

### Высокая загрузка CPU
Скрипт оптимизирован для работы в фоне. В Docker-контейнере потребление CPU минимально.

## 📄 Лицензия

MIT License - свободное использование и модификация.

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для вашей функции
3. Зафиксируйте изменения
4. Отправьте pull request

## 📞 Поддержка

- Создайте Issue в GitHub для багов и предложений
- Проверьте документацию по устранению неполадок выше
